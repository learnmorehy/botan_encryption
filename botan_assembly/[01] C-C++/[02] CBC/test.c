#include <stdio.h>

int main() {
    int dividend = 17;
    int divisor = 5;
    
    int result = dividend % divisor;

    printf("The result of %d %% %d is %d\n", dividend, divisor, result);

    return 0;
}

